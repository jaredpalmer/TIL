You can print out all the words in your dictionary that start with any string using this terminal command:

```bash
cat /usr/share/dict/words | grep "^re" | less
```

Good for finding names for projects.
